<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.act.member.IJoinMemberDAO">

	<select id="searchMemsid" resultType="java.lang.String">
		SELECT NVL(MAX(MEM_SID), '0') AS MEM_SID
		FROM JOIN_MEMBER
		WHERE JM_ID = #{jmId} AND JM_PW = #{jmPw}
	</select>

	<select id="findId" resultType="java.lang.String">
		SELECT NVL(MAX(JM_ID), '0') AS JM_ID
		FROM JOIN_MEMBER
		WHERE JM_NAME = #{jmName} AND JM_SSN = #{jmSsn}
	</select>
	
	<select id="findPw" resultType="java.lang.Integer">
		SELECT NVL( (SELECT JM_SID
            		FROM JOIN_MEMBER
            		WHERE JM_ID = #{jmId} AND JM_SSN = #{jmSsn} AND JM_NAME = #{jmName}), 0) AS JM_SID
		FROM DUAL
	</select>

</mapper>
